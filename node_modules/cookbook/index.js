//cookbook module!!!!!

module.exports = {
    user_login: function(req,res,results,index,callback) {
          var obj = results[index];
          console.log(obj);;

          console.log("User login : " + obj.voornaam);
          req.session.voornaam = obj.voornaam;
          req.session.achternaam = obj.achternaam;
          req.session.gebruikersnaam = obj.gebruikersnaam;
          req.session.level = obj.level;
          req.session.userID = obj.ID;
    },
    user_logout: function(results,callback) {
        console.log("User logout : " + user);
    },
    user_not_defined: function(req,res) {
      req.session.level = '0';
    },
    time : function(){
      var date = new Date();
      var hour = date.getHours();
      hour = (hour < 10 ? "0" : "") + hour;
      var min  = date.getMinutes();
      min = (min < 10 ? "0" : "") + min;
      var sec  = date.getSeconds();
      sec = (sec < 10 ? "0" : "") + sec;
      var year = date.getFullYear();
      var month = date.getMonth() + 1;
      month = (month < 10 ? "0" : "") + month;
      var day  = date.getDate();
      day = (day < 10 ? "0" : "") + day;
      return year + month + day + hour + min + sec;
    }
};
